// Demo categories that ship with the DTC starter template's seed data —
// not part of this store's real catalog, so they're excluded from
// customer-facing category lists (Store filters, quick-access strips,
// footer links) wherever categories are surfaced.
export const EXCLUDED_CATEGORY_HANDLES = [
  "shirts",
  "merch",
  "sweatshirts",
  "pants",
]
